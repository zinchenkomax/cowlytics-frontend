import { EditOnEnterDirective } from './edit-on-enter.directive';

describe('EditOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new EditOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
